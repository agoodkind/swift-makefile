name: Dependabot Auto Merge

on:
  workflow_run:
    workflows:
      - CI
    types:
      - completed

jobs:
  automerge:
    uses: ./.github/workflows/_dependabot-automerge.yml
    permissions:
      contents: write
      pull-requests: write
    secrets: inherit
