.make/
.build/
.swiftpm/
swiftcheck/Package.resolved
build/
# Track the docs build area; the broad build/ rule above is for compiler output.
!docs/build/
Products/
Derived/
DerivedData/
*.xcodeproj
*.xcworkspace
.sdd-briefs/
.superpowers/
# The ubuntu changes job checks the engine out here to build swift-mk from one
# uniform path; keep this runtime checkout out of the repo's own status and lint set.
.swift-makefile/
